.git-publish/
.atl/
node_modules/
.env
.DS_Store
coverage/

# Experimental scratch space — variant labs, dogfood runs, throwaway projects.
# Excluded from the repo on purpose (norms live in skills/ + overlay/, not here).
workspaces/
